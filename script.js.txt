// script.js
function sl(inp, vid, fid) {
  const v = inp.value;
  document.getElementById(vid).textContent = v;
  document.getElementById(fid).style.width = ((v - 1) / 9 * 100) + '%';
}

function openTab(id, btn) {
  document.querySelectorAll('.tpanel').forEach(p => p.classList.remove('on'));
  document.querySelectorAll('.tbtn').forEach(b => b.classList.remove('on'));
  document.getElementById(id).classList.add('on');
  btn.classList.add('on');
}

// Presets
const PRESETS = { /* ... mismo contenido que en el original ... */ };

function loadPreset(k) {
  const p = PRESETS[k];
  // ... mismo código ...
}

function currSym() {
  // ... mismo código ...
}

function calcStake(realProbStr, oddStr, confidence) {
  // ... mismo código ...
}

let sTimer = null;
function startSteps() { /* ... */ }
function doneSteps() { /* ... */ }

function cringHTML(score) { /* ... */ }

// System prompt y funciones principales
const SYS = `Eres el motor de predicción de BET GENIUS...`; // (completo)

function buildPrompt() { /* ... */ }
function parseJ(raw) { /* ... */ }
function render(data) { /* ... */ }

async function analyze() {
  // ... toda la función analyze() ...
}

function showErr(msg) {
  const el = document.getElementById('errbox');
  el.textContent = '⚠ ' + msg;
  el.className = 'errbox on';
}

document.addEventListener('DOMContentLoaded', () => {
  document.getElementById('matchdate').valueAsDate = new Date();
});